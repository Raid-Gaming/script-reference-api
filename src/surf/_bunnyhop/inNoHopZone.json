{
  "sourceFile": "surf\\_bunnyhop",
  "usage": "<player> inNoHopZone()",
  "description": ["Checks if a player is in a zone where bunny hopping is disallowed."],
  "return": "bool",
  "example": [
    "if (self inNoHopZone() && playerSpeed > 320) {",
    "  playerSpeed = 280;",
    "  playerVel = vectorScale(vectorNormalize(playerVel), playerSpeed);",
    "}"
  ]
}
